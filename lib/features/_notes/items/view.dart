import 'package:flutter/material.dart';

class ActionView extends StatelessWidget {
  const ActionView({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
